# ЛР4 — Apache JMeter

Тест-план: главная страница Яндекса и переход по случайному разделу (Random Controller).

## Запуск

```bash
jmeter -n -t testplan.jmx -l results.jtl
jmeter -g results.jtl -o report      # HTML-отчёт с графиками
```

## Результат прогона

```
summary = 20 in 00:00:10 = 1.9/s  Avg: 900  Min: 149  Max: 2249  Err: 0 (0.00%)
```

Всего 65 запросов (20 основных + 45 по перенаправлениям), ошибок 0.
Время отклика: медиана 334 мс, 90 % — 1109 мс, 95 % — 1656 мс.

## Найденный дефект

Первый прогон дал 3 ошибки: раздел «Переводчик» отвечал кодом 404 —
путь `yandex.ru/translate` больше не существует, раздел вынесен на поддомен.
Sampler исправлен на `translate.${SERVER}/`, повторный прогон прошёл без ошибок.

`results.jtl` — результаты итогового прогона.
