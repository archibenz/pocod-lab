# ПР1 — Установка Docker

Развёртывание среды контейнеризации и проверка работоспособности.

```
docker --version          → Docker version 29.2.1, build a5c7197
docker run hello-world    → Hello from Docker!
```

Среда выполнения: ядро Linux 6.12.76-linuxkit, архитектура arm64.
